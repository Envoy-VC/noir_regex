pub global MAX_PATTERN_LENGTH: u32 = 256; // Maximum length of the regex pattern
pub global MAX_INPUT_LENGTH: u32 = 1024; // Maximum length of the input string
pub global MAX_AST_NODES: u32 = 512; // Maximum number of AST nodes
pub global MAX_NFA_STATES: u32 = 1024; // Maximum number of NFA states
pub global MAX_DFA_STATES: u32 = 2048; // Maximum number of DFA states
pub global MAX_TRANSITIONS: u32 = 4096; // Maximum number of transitions
pub global STACK_SIZE: u32 = 1024; // Fixed stack size
pub global QUEUE_SIZE: u32 = 2048; // Fixed queue size
